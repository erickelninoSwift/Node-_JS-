const encrypte = (data) => {
  return "Data is Encrypted : " + data;
};
function send(url, data) {
  console.log(encrypte(data) + `is being send to this URL: ${url}`);
}

module.exports = {
  send: send,
};
