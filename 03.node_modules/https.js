const { request: requesting } = require("./internals");
const { response } = require("./internals");

const request = (url, data) => {
  requesting.send(url, data);
  response.read(url, data);
};

request("www.googl.com", "ronaldo");
